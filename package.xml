<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>testudo</name>
  <version>0.0.1</version>
  <description>Black-box diagnostics tool for navigation stacks (Nav2-first).</description>
  <maintainer email="elechim2196@gmail.com">mlisi1</maintainer>
  <license>Apache-2.0</license>

  <depend>rclpy</depend>
  <depend>diagnostic_msgs</depend>
  <depend>lifecycle_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav2_msgs</depend>
  <depend>action_msgs</depend>
  <depend>tf2_msgs</depend>
  <depend>ament_index_python</depend>
  <depend>rosidl_runtime_py</depend>

  <exec_depend>python3-yaml</exec_depend>
  <exec_depend>python3-numpy</exec_depend>
  <!-- Only needed for `testudo replay`; imported lazily so other commands
       don't require it. -->
  <exec_depend>rosbag2_py</exec_depend>
  <!-- rosdep resolves this to an apt package stuck at 0.1.13 (2022), far
       too old for the reactive-widget/DataTable APIs `testudo watch` uses
       (setup.py's install_requires pulls a modern one via pip instead).
       Declared here for ROS packaging convention; `pip install textual`
       is what actually needs to happen on a rosdep-based install. -->
  <exec_depend>python3-textual</exec_depend>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
